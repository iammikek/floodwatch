APP_ENV=testing
APP_DEBUG=true

# Test database: PostgreSQL (matches prod). Sail: host=pgsql; CI: workflow overrides with host=127.0.0.1
DB_CONNECTION=pgsql
DB_HOST=pgsql
DB_PORT=5432
DB_DATABASE=testing
DB_USERNAME=sail
DB_PASSWORD=password
